--index-url https://download.pytorch.org/whl/cu128
--extra-index-url https://pypi.org/simple
torch==2.10
triton

numpy
huggingface_hub
# The classic model-type copies of kernels-community's kernels were removed from the hub
# on 2026-09-14; kernels >= 0.14 loads from the kernel-type repos instead, via
# get_kernel(..., version=<major>). 0.17.0 validated (the same flash-attn2 build).
kernels==0.17.0
wandb
pyarrow
tokenizers
rustbpe
tiktoken
filelock
jinja2
requests
tqdm
datasets
